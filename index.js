// Function expression
const divide = function(a, b) {
  return a / b;
};

// Arrow function that squares a number
const square = x => x * x;

// Arrow function that adds two numbers
const add = (a, b) => a + b;
